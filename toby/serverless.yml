org:
app:
service: toby

frameworkVersion: '3'

provider:
  name: aws
  runtime: python3.8

functions:
  server_commands:
    handler: handler.server_commands
    events:
      - http:
          path: /hello
          method: get
          cors: true
          private: true
